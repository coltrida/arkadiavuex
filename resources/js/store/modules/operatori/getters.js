export default {
    presenze(state) {
        return state.presenze;
    },

    operatori(state) {
        return state.operatori;
    },

    settimanaAttuale(state) {
        return state.settimanaAttuale;
    },

    settimane(state) {
        return state.settimane;
    },

    statistiche(state){
        return state.statistiche;
    },

    totore(state){
        return state.totore;
    },

    operatore(state) {
        return state.operatore;
    },

    load(state) {
        return state.load;
    },
};
